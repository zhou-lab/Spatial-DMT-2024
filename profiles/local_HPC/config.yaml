default-resources:
  mem: "10G"
  cpus_per_task: 1
cores: 8
printshellcmds: True
latency-wait: 100
rerun-triggers:
  - mtime
## Scalar form (not list) -- profile-dir-relative resolution. See slurm/config.yaml.
configfile: site.yaml
